post:
  summary: Оплатить заказ
  description: Проводит оплату ранее созданного заказа
  operationId: payOrder
  parameters:
    - $ref: "../params/order_uuid.yaml"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../components/pay_order_request.yaml"
  responses:
    "200":
      description: Оплата прошла успешно
      content:
        application/json:
          schema:
            $ref: "../components/pay_order_response.yaml"
    "400":
      description: Неверный запрос
      content:
        application/json:
          schema:
            $ref: "../components/errors/bad_request_error.yaml"
    "404":
      description: Заказ не найден
      content:
        application/json:
          schema:
            $ref: "../components/errors/not_found_error.yaml"
    "409":
      description: Заказ уже оплачен или не может быть оплачен
      content:
        application/json:
          schema:
            $ref: "../components/errors/conflict_error.yaml"
    "500":
      description: Ошибка при оплате
      content:
        application/json:
          schema:
            $ref: "../components/errors/internal_server_error.yaml"